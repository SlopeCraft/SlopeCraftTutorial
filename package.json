{
  "devDependencies": {
    "vuepress": "^1.9.7",
    "vuepress-plugin-mathjax": "^1.2.8"
  },
  "scripts": {
    "dev": "vuepress dev .",
    "dev-v17-win": "set NODE_OPTIONS=--openssl-legacy-provider && vuepress dev .",
    "build": "vuepress build .",
    "build-v17": "export NODE_OPTIONS=--openssl-legacy-provider && vuepress build ."
  }
}
